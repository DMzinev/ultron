TASK_ID: 'Task-ImpactSimulator'
TASK_TYPE: 'LOGIC_CHANGE'
BUILDER_VERSION: 'v2.0'
COMMIT_HASH: '3c3a975c5bdee9ddf8b1b1e0fd3aef6674f3f4e1'
PLAN_HASH: '2280396054635135715'
CHANGED_FILES:
- 'ultron/experimental/impact_simulator.py'
- 'ultron/tests/run_tests.py'
PATCH_DIFF: |
  diff --git a/ultron/experimental/impact_simulator.py b/ultron/experimental/impact_simulator.py
  new file mode 100644
  index 0000000..8c65660
  --- /dev/null
  +++ b/ultron/experimental/impact_simulator.py
  @@ -0,0 +1,103 @@
  +from dataclasses import dataclass
  +from typing import List
  +
  +
  +@dataclass
  +class MetricSnapshot:
  +    """
  +    Represents a snapshot of key codebase quality metrics.
  +    """
  +    total_coupling_debt: float
  +    total_cycle_count: int
  +    total_violations: int
  +    avg_instability: float
  +    avg_hotspot_score: float
  +
  +    def __post_init__(self):
  +        # Type validations
  +        if not isinstance(self.total_coupling_debt, (int, float)):
  +            raise TypeError("total_coupling_debt must be a float or integer")
  +        if not isinstance(self.total_cycle_count, int):
  +            raise TypeError("total_cycle_count must be an integer")
  +        if not isinstance(self.total_violations, int):
  +            raise TypeError("total_violations must be an integer")
  +        if not isinstance(self.avg_instability, (int, float)):
  +            raise TypeError("avg_instability must be a float or integer")
  +        if not isinstance(self.avg_hotspot_score, (int, float)):
  +            raise TypeError("avg_hotspot_score must be a float or integer")
  +
  +        # Bounds validation
  +        if self.total_coupling_debt < 0.0:
  +            raise ValueError("total_coupling_debt must be non-negative")
  +        if self.total_cycle_count < 0:
  +            raise ValueError("total_cycle_count must be non-negative")
  +        if self.total_violations < 0:
  +            raise ValueError("total_violations must be non-negative")
  +        if not (0.0 <= self.avg_instability <= 1.0):
  +            raise ValueError("avg_instability must be in range [0.0, 1.0]")
  +        if not (0.0 <= self.avg_hotspot_score <= 1.0):
  +            raise ValueError("avg_hotspot_score must be in range [0.0, 1.0]")
  +
  +        # Ensure types are cast correctly
  +        self.total_coupling_debt = float(self.total_coupling_debt)
  +        self.avg_instability = float(self.avg_instability)
  +        self.avg_hotspot_score = float(self.avg_hotspot_score)
  +
  +
  +@dataclass
  +class SimulationResult:
  +    """
  +    Wrapper for before and after metrics snapshots.
  +    """
  +    before: MetricSnapshot
  +    after: MetricSnapshot
  +
  +
  +class ImpactSimulator:
  +    """
  +    Projects how codebase quality metrics change based on refactoring deltas.
  +    """
  +
  +    def __init__(self, baseline: MetricSnapshot, recommendations: list):
  +        if not isinstance(baseline, MetricSnapshot):
  +            raise TypeError("baseline must be a MetricSnapshot instance")
  +        if not isinstance(recommendations, list):
  +            raise TypeError("recommendations must be a list")
  +        self.baseline = baseline
  +        self.recommendations = recommendations
  +
  +    def simulate(self) -> SimulationResult:
  +        """
  +        Projects new metrics by subtracting expected deltas with defensive clamping.
  +        """
  +        assert self is not None, "Simulator instance cannot be None"
  +        projected_debt = self.baseline.total_coupling_debt
  +        projected_cycles = self.baseline.total_cycle_count
  +        projected_violations = self.baseline.total_violations
  +
  +        for rec in self.recommendations:
  +            delta = getattr(rec, "expected_delta", {})
  +            #expected_delta keys validation
  +            d_debt = delta.get("coupling_debt", 0.0)
  +            d_cycles = delta.get("cycle_count", 0)
  +            d_violations = delta.get("violations_resolved", 1)
  +
  +            # subtract the expected deltas (deltas are negative, so subtracting reduces the metric)
  +            projected_debt += d_debt
  +            projected_cycles += d_cycles
  +            projected_violations -= d_violations
  +
  +        # Defensive bounds clamping to physical limits to prevent out-of-range ValueErrors
  +        projected_debt = max(0.0, float(projected_debt))
  +        projected_cycles = max(0, int(projected_cycles))
  +        projected_violations = max(0, int(projected_violations))
  +
  +        after_snapshot = MetricSnapshot(
  +            total_coupling_debt=projected_debt,
  +            total_cycle_count=projected_cycles,
  +            total_violations=projected_violations,
  +            avg_instability=self.baseline.avg_instability,
  +            avg_hotspot_score=self.baseline.avg_hotspot_score,
  +        )
  +
  +        return SimulationResult(before=self.baseline, after=after_snapshot)
  diff --git a/ultron/tests/run_tests.py b/ultron/tests/run_tests.py
  index 812db02..14c8430 100644
  --- a/ultron/tests/run_tests.py
  +++ b/ultron/tests/run_tests.py
  @@ -1789,6 +1789,120 @@ class TestRecommendationEngine(unittest.TestCase):
               Recommendation("f.py", "ADP", "smell", "refact", {}, 1, 0)
   
   
  +
  +class TestImpactSimulator(unittest.TestCase):
  +
  +    def test_metric_snapshot_invalid_types_raises_type_error(self):
  +        from impact_simulator import MetricSnapshot
  +        with self.assertRaises(TypeError):
  +            MetricSnapshot("not_a_float", 0, 0, 0.5, 0.5)
  +        with self.assertRaises(TypeError):
  +            MetricSnapshot(10.5, "not_an_int", 0, 0.5, 0.5)
  +        with self.assertRaises(TypeError):
  +            MetricSnapshot(10.5, 0, "not_an_int", 0.5, 0.5)
  +
  +    def test_metric_snapshot_invalid_bounds_raises_value_error(self):
  +        from impact_simulator import MetricSnapshot
  +        # negative coupling debt
  +        with self.assertRaises(ValueError):
  +            MetricSnapshot(-1.0, 0, 0, 0.5, 0.5)
  +        # negative cycles
  +        with self.assertRaises(ValueError):
  +            MetricSnapshot(10.0, -1, 0, 0.5, 0.5)
  +        # negative violations
  +        with self.assertRaises(ValueError):
  +            MetricSnapshot(10.0, 0, -1, 0.5, 0.5)
  +        # instability out of [0, 1]
  +        with self.assertRaises(ValueError):
  +            MetricSnapshot(10.0, 0, 0, 1.1, 0.5)
  +        # hotspot out of [0, 1]
  +        with self.assertRaises(ValueError):
  +            MetricSnapshot(10.0, 0, 0, 0.5, -0.1)
  +
  +    def test_metric_snapshot_valid_casting(self):
  +        from impact_simulator import MetricSnapshot
  +        snap = MetricSnapshot(10, 2, 3, 0.4, 0.6)
  +        self.assertIsInstance(snap.total_coupling_debt, float)
  +        self.assertEqual(snap.total_coupling_debt, 10.0)
  +
  +    def test_impact_simulator_invalid_init_raises_type_error(self):
  +        from impact_simulator import ImpactSimulator
  +        with self.assertRaises(TypeError):
  +            ImpactSimulator("not_a_snapshot", [])
  +
  +    def test_impact_simulator_empty_recommendations_boundary(self):
  +        from impact_simulator import MetricSnapshot, ImpactSimulator
  +        snap = MetricSnapshot(10.0, 2, 3, 0.4, 0.6)
  +        sim = ImpactSimulator(snap, [])
  +        res = sim.simulate()
  +        self.assertEqual(res.after.total_coupling_debt, 10.0)
  +        self.assertEqual(res.after.total_cycle_count, 2)
  +        self.assertEqual(res.after.total_violations, 3)
  +
  +    def test_impact_simulator_correct_simulation_math(self):
  +        from impact_simulator import MetricSnapshot, ImpactSimulator
  +        from recommendation_engine import Recommendation
  +        snap = MetricSnapshot(25.0, 5, 4, 0.4, 0.6)
  +        recs = [
  +            Recommendation("a.py", "ADP", "circular_dependency", "fix", {"coupling_debt": -15.0, "cycle_count": -1, "violations_resolved": 1}, 1, 1),
  +            Recommendation("b.py", "DIP", "high_fan_out", "fix", {"coupling_debt": -5.0, "cycle_count": 0, "violations_resolved": 1}, 3, 2)
  +        ]
  +        sim = ImpactSimulator(snap, recs)
  +        res = sim.simulate()
  +        # coupling debt: 25.0 + (-15.0) + (-5.0) = 5.0
  +        self.assertEqual(res.after.total_coupling_debt, 5.0)
  +        # cycles: 5 + (-1) = 4
  +        self.assertEqual(res.after.total_cycle_count, 4)
  +        # violations: 4 - 2 = 2
  +        self.assertEqual(res.after.total_violations, 2)
  +
  +    def test_impact_simulator_clamping_prevents_negative_values(self):
  +        from impact_simulator import MetricSnapshot, ImpactSimulator
  +        from recommendation_engine import Recommendation
  +        snap = MetricSnapshot(10.0, 1, 1, 0.4, 0.6)
  +        recs = [
  +            Recommendation("a.py", "ADP", "circular_dependency", "fix", {"coupling_debt": -15.0, "cycle_count": -2, "violations_resolved": 1}, 1, 1),
  +            Recommendation("b.py", "DIP", "high_fan_out", "fix", {"coupling_debt": -5.0, "cycle_count": 0, "violations_resolved": 1}, 3, 2)
  +        ]
  +        sim = ImpactSimulator(snap, recs)
  +        res = sim.simulate()
  +        self.assertEqual(res.after.total_coupling_debt, 0.0)
  +        self.assertEqual(res.after.total_cycle_count, 0)
  +        self.assertEqual(res.after.total_violations, 0)
  +
  +    def test_simulation_result_dataclass(self):
  +        from impact_simulator import MetricSnapshot, SimulationResult
  +        snap1 = MetricSnapshot(10.0, 1, 1, 0.4, 0.6)
  +        snap2 = MetricSnapshot(5.0, 0, 0, 0.4, 0.6)
  +        res = SimulationResult(before=snap1, after=snap2)
  +        self.assertEqual(res.before, snap1)
  +        self.assertEqual(res.after, snap2)
  +
  +    def test_impact_simulator_clamping_floats_and_integers(self):
  +        from impact_simulator import MetricSnapshot, ImpactSimulator
  +        from recommendation_engine import Recommendation
  +        # If expected_delta yields non-integral float values for cycles, they must be clamped/cast to integer
  +        snap = MetricSnapshot(10.0, 5, 2, 0.4, 0.6)
  +        recs = [
  +            Recommendation("a.py", "ADP", "circular_dependency", "fix", {"coupling_debt": -5.5, "cycle_count": -2, "violations_resolved": 1}, 1, 1)
  +        ]
  +        sim = ImpactSimulator(snap, recs)
  +        res = sim.simulate()
  +        self.assertIsInstance(res.after.total_coupling_debt, float)
  +        self.assertEqual(res.after.total_coupling_debt, 4.5)
  +        self.assertIsInstance(res.after.total_cycle_count, int)
  +        self.assertEqual(res.after.total_cycle_count, 3)
  +
  +    def test_impact_simulator_avg_metrics_carried_forward(self):
  +        from impact_simulator import MetricSnapshot, ImpactSimulator
  +        # Instability and hotspot scores must remain unchanged
  +        snap = MetricSnapshot(10.0, 5, 2, 0.35, 0.75)
  +        sim = ImpactSimulator(snap, [])
  +        res = sim.simulate()
  +        self.assertEqual(res.after.avg_instability, 0.35)
  +        self.assertEqual(res.after.avg_hotspot_score, 0.75)
  +
  +
   if __name__ == "__main__":
       print("[+] Running Ultron Core Tests...")
       unittest.main()
EXPECTED_OUTCOMES: 'MetricSnapshot with type and bounds validation; SimulationResult; ImpactSimulator projecting delta coupling_debt, cycle_count, and violations_resolved with max(0) clamping; instability and hotspot scores carried forward; 10 unit tests (117 total OK)'
TEST_COMMANDS:
- 'python ultron/tests/run_tests.py'
RAW_STDOUT: |
  [+] Running Ultron Core Tests...
  [+] Recorded rating for ultron/core/risk/scoring.py by CLI_Test.
    - Rater Judgment: HIGH
    - System Computed: HIGH (Score: 97.25)
    - Agreement: MATCH
  [Ultron] No git history found — bug-prone-file scaling is inactive.
  [Ultron] No git history found — bug-prone-file scaling is inactive.
  Usage: python checks.py <file_or_dir_path>
  Usage: python failure_space.py <repo_path> <comma_separated_changed_files>
  [*] Starting calibration over 1 transactions...
  [+] Recalibration complete.
    - New Fusion Weights: w_test=0.341, w_git=0.259, w_runtime=0.141, w_human=0.059, w_test_runtime=0.094, w_git_human=0.106
RAW_STDERR: |
  ...................................................................[-] RecommendationEngine: skipping card with missing principle or filepath.
  [-] RecommendationEngine: skipping card with missing principle or filepath.
  [-] RecommendationEngine: unrecognized principle 'Unrecognized Principle' – skipping.
  .......................[-] Error processing rating: Could not evaluate risks for file
  .....127.0.0.1 - - [06/Jul/2026 13:20:33] "POST /api/design-oracle HTTP/1.1" 200 -
  127.0.0.1 - - [06/Jul/2026 13:20:33] "POST /api/design-oracle HTTP/1.1" 400 -
  C:\Users\This PC\AppData\Local\Python\pythoncore-3.14-64\Lib\tempfile.py:484: ResourceWarning: Implicitly cleaning up <HTTPError 400: 'Bad Request'>
    _warnings.warn(self.warn_message, ResourceWarning)
  127.0.0.1 - - [06/Jul/2026 13:20:33] "POST /api/design-oracle HTTP/1.1" 400 -
  127.0.0.1 - - [06/Jul/2026 13:20:33] "POST /api/design-oracle HTTP/1.1" 200 -
  127.0.0.1 - - [06/Jul/2026 13:20:33] "POST /api/design-oracle HTTP/1.1" 200 -
  127.0.0.1 - - [06/Jul/2026 13:20:33] "POST /api/design-oracle HTTP/1.1" 200 -
  127.0.0.1 - - [06/Jul/2026 13:20:33] "POST /api/design-oracle HTTP/1.1" 400 -
  127.0.0.1 - - [06/Jul/2026 13:20:33] "POST /api/design-oracle HTTP/1.1" 400 -
  ................[+] Ultron: Generating codebase context brief...
  ......
  ----------------------------------------------------------------------
  Ran 117 tests in 72.443s
  
  OK
SELF_AUDIT: ''
KNOWN_LIMITATIONS: 'Deltas are theoretical, best-case projections assuming perfect, isolated application — not actual post-refactoring forecasts; average instability/hotspots static'
CATEGORY_B: 'false'
