TASK_ID: 'Task-Preflight'
TASK_TYPE: 'LOGIC_CHANGE'
BUILDER_VERSION: 'v2.0'
COMMIT_HASH: '34063b6f8d1039d4b9bcf50afdf72125194e227c'
PLAN_HASH: '4813598278599314302'
CHANGED_FILES:
- 'PROTOCOL.md'
- 'ROADMAP.md'
- 'umags/governor.py'
- 'umags/run_verification_loop.py'
PATCH_DIFF: |
  diff --git a/PROTOCOL.md b/PROTOCOL.md
  index 5d175e9..a0a0d2b 100644
  --- a/PROTOCOL.md
  +++ b/PROTOCOL.md
  @@ -130,3 +130,86 @@ If omitted, the loop defaults to `LOGIC_CHANGE` (conservative).
   **Read the task. Say what you're about to do before doing it. If it
   needs a real human, stop and say so. Show real output. Check it against
   the rules. Log it fully. Don't move on until that's done.**
  +
  +---
  +
  +## Efficiency & Escalation: The Pre-flight Gate
  +
  +### Rule (permanent constraint, not a one-off patch)
  +
  +Before any Builder/Auditor/Judge/Historian steps in `run_verification_loop.py`,
  +the loop calls Ultron's own `risk.evaluate_risks()` on the declared target files.
  +This is a local, no-LLM, O(1)-in-ceremony-cost operation. The tier it returns
  +decides how much verification ceremony the rest of the loop executes.
  +
  +**The tier ordering is: HIGH > MEDIUM > LOW.**
  +(Never use string `max()` â€” `"MEDIUM" > "LOW" > "HIGH"` lexicographically,
  +which is wrong. The implementation uses `_TIER_ORDER = {"LOW": 0, "MEDIUM": 1, "HIGH": 2}`.)
  +
  +### FAST PATH â€” `LOW/MEDIUM + STRUCTURE_ONLY + no Category-B`
  +
  +Checks run:
  +- Scope verification (undeclared source files check)
  +- Single test suite run
  +
  +Explicitly skipped:
  +- O(n) nullification loop
  +- Per-file AST drift and residual risk analysis
  +- Cognitive LLM review
  +
  +Telemetry is written. The loop proceeds through all four roles (B/A/J/H)
  +but those roles perform only the fast-path subset of checks.
  +
  +> [!NOTE]
  +> FAST PATH does not skip the independent Critic subagent review â€”
  +> that review happens at *planning time*, before governor is invoked.
  +> UMAGS Â§1 is satisfied by the planning-time Critic, not by the loop.
  +
  +### FULL PATH â€” `HIGH tier`, OR `Category-B=true`, OR `LOGIC_CHANGE`
  +
  +All existing checks run. At the end of the loop, if `tier=HIGH` or
  +`category_b=true`, the loop prints:
  +
  +```
  +ESCALATE: requires external review
  +```
  +
  +This means: paste the full output into `PROJECT_LOG.md` under the
  +`External verification` field. Only a human operator may fill that field.
  +No agent in the pipeline may generate, simulate, or pre-fill it.
  +
  +### The O(n) Token-Budget Rule (permanent, enforced in code)
  +
  +> **No check may scale with file count (O(n) per-file loops) unless the
  +> pre-flight tier is HIGH.**
  +
  +The nullification loop is the primary O(n) offender (it runs tests once
  +per changed file). It only executes when `preflight_tier == "HIGH"`.
  +At LOW or MEDIUM tier, it is skipped with an explicit log line explaining
  +the reason. This is enforced in `run_verification_loop.py`, not advisory.
  +
  +### Fail-safe (hard rule, no exceptions)
  +
  +If the pre-flight gate itself throws an exception for any reason, the tier
  +defaults to `HIGH` and the full path runs. The system **never** silently
  +auto-approves on uncertainty. Uncertainty escalates â€” it does not wave through.
  +
  +If the gate returns an empty risks list (non-Python targets, new files not
  +yet in the codebase scan), the tier is `LOW`. This is a valid result â€”
  +non-Python files have no coupling risk to the governance engine.
  +
  +### Category-B flag
  +
  +Any task containing numbers derived from data (precision, recall, F1,
  +rating comparisons, calibration scores, etc.) must pass `--category-b`
  +to `governor.py`. This stamps `CATEGORY_B: 'true'` in `audit_package.yaml`
  +and forces the full verification path plus an `ESCALATE` notice, regardless
  +of the pre-flight risk tier.
  +
  +### Path normalization requirement
  +
  +`evaluate_risks()` silently skips files whose paths do not match the keys
  +in the codebase dict produced by `analyze_directory()`. On Windows, declared
  +paths may use backslashes while codebase keys use forward slashes. The
  +pre-flight gate must normalize both to forward-slash relative paths before
  +comparison. This is implemented in `run_preflight_risk_gate()`.
  diff --git a/ROADMAP.md b/ROADMAP.md
  index 6c892b4..42ebe4b 100644
  --- a/ROADMAP.md
  +++ b/ROADMAP.md
  @@ -122,6 +122,20 @@ study to use it properly (see Phase 1 plan).
   
   ---
   
  +## ðŸ”‡ DORMANT â€” Working code, not in active use
  +
  +### AI Rater + Compare AI Ratings (`ultron/validation/ai_rater.py`, `umags/tools/compare_ai_ratings.py`)
  +Both files are syntactically valid and were written for an AI-vs-human rating comparison
  +study. Neither is currently called by any active pipeline. The comparison study they support
  +is BLOCKED on the human feedback collection step (see `blind_rate.py` above â€” the
  +`blind_feedback.jsonl` file is still empty).
  +
  +**Decision (2026-06-24):** Kept in place, not deleted. If human ratings are collected and
  +the pipeline resumes, these are the correct next step. Removing them would require
  +re-implementation. Marked dormant, not dead. Do not touch without a specific plan.
  +
  +---
  +
   ## ðŸª¦ Documented, not implemented
   
   The following are described in `research-notes/speculative-ideas.md` but have
  diff --git a/umags/governor.py b/umags/governor.py
  index 1173572..c542046 100644
  --- a/umags/governor.py
  +++ b/umags/governor.py
  @@ -72,6 +72,16 @@ def main():
               "no AST drift, no architecture drift. Use only when no file content changed (git mv / rename only)."
           )
       )
  +    parser.add_argument(
  +        "--category-b",
  +        action="store_true",
  +        default=False,
  +        help=(
  +            "Flag this task as containing Category-B data claims (numbers derived from data: "
  +            "precision, recall, F1, rating comparisons, etc.). "
  +            "Forces full verification path and an ESCALATE notice requiring external human review."
  +        )
  +    )
       args = parser.parse_args()
   
       repo_path = os.path.abspath(os.path.join(os.path.dirname(os.path.abspath(__file__)), ".."))
  @@ -115,7 +125,8 @@ def main():
           "RAW_STDOUT": stdout,
           "RAW_STDERR": stderr,
           "SELF_AUDIT": args.self_audit,
  -        "KNOWN_LIMITATIONS": args.limitations
  +        "KNOWN_LIMITATIONS": args.limitations,
  +        "CATEGORY_B": "true" if args.category_b else "false"
       }
   
       # Write as YAML to ultron/meta/audit_package.yaml
  diff --git a/umags/run_verification_loop.py b/umags/run_verification_loop.py
  index 9d4b9ed..915e7be 100644
  --- a/umags/run_verification_loop.py
  +++ b/umags/run_verification_loop.py
  @@ -422,6 +422,55 @@ def parse_patch_diff_lines(patch_diff):
                   current_line += 1
       return changed_lines
   
  +
  +# ---------------------------------------------------------------------------
  +# PRE-FLIGHT RISK GATE â€” Ultron self-scan, no LLM, zero O(n) cost.
  +# Called from main() after sys.path is configured and changed_files is loaded.
  +# ---------------------------------------------------------------------------
  +_TIER_ORDER = {"LOW": 0, "MEDIUM": 1, "HIGH": 2}
  +
  +def run_preflight_risk_gate(repo_path, changed_files):
  +    """
  +    Calls Ultron's risk.evaluate_risks() on the declared target files.
  +    Returns 'HIGH', 'MEDIUM', or 'LOW'.
  +
  +    Fail-safe: any exception returns 'HIGH' (never auto-approve on uncertainty).
  +    Empty risks list (non-Python targets, new files) returns 'LOW' â€” valid
  +    result because no Python code targets means no governance risk.
  +
  +    Path normalization: changed_files paths are normalized to forward-slash
  +    relative paths before lookup, matching analyzer.analyze_directory() keys
  +    on both Windows and POSIX.
  +    """
  +    try:
  +        import analyzer as _analyzer_mod
  +        import risk as _risk_mod
  +        # Normalize declared paths to match codebase dict keys produced by
  +        # analyze_directory (forward-slash relative paths from repo root).
  +        norm_targets = [
  +            os.path.normpath(f).replace("\\", "/") for f in changed_files
  +        ]
  +        codebase = _analyzer_mod.analyze_directory(repo_path)
  +        # Build a forward-slash keyed lookup from the codebase dict.
  +        # analyze_directory may return OS-native paths; normalize them too.
  +        norm_codebase = {
  +            os.path.normpath(k).replace("\\", "/"): v
  +            for k, v in codebase.items()
  +        }
  +        risks = _risk_mod.evaluate_risks(norm_codebase, norm_targets, repo_path=repo_path)
  +        if not risks:
  +            # No Python files matched â€” docs, configs, or new files not yet
  +            # in the codebase scan.  Treat as LOW (no coupling to assess).
  +            return "LOW"
  +        # Use explicit tier ordering: HIGH=2 > MEDIUM=1 > LOW=0.
  +        # Never use string max() â€” "MEDIUM" > "LOW" > "HIGH" lexicographically.
  +        best = max(risks, key=lambda r: _TIER_ORDER.get(r.level, 2))
  +        return best.level
  +    except Exception as e:
  +        print(f"[!] PRE-FLIGHT ERROR: risk gate threw exception ({e}) â€” defaulting to HIGH (fail-safe).")
  +        return "HIGH"
  +
  +
   def main():
       import sys
       if hasattr(sys.stdout, "reconfigure"):
  @@ -455,7 +504,50 @@ def main():
           task_type = "LOGIC_CHANGE"
       changed_files = package.get("CHANGED_FILES", [])
       test_commands = package.get("TEST_COMMANDS", [])
  -    
  +
  +    # =========================================================================
  +    # 0. PRE-FLIGHT RISK GATE
  +    # Runs before Builder/Auditor/Judge/Historian steps.
  +    # Determines ceremony level using Ultron's own risk engine (no LLM, no human).
  +    # Fail-safe: gate error â†’ HIGH (never silently auto-approve on uncertainty).
  +    # =========================================================================
  +    print("====================================================================")
  +    print("ðŸ›«  PRE-FLIGHT RISK GATE (Ultron self-scan)")
  +    print("====================================================================")
  +    category_b = package.get("CATEGORY_B", "false").strip().lower() == "true"
  +    print(f"[*] Pre-flight: Scanning {len(changed_files)} target file(s) via risk.evaluate_risks()...")
  +    preflight_tier = run_preflight_risk_gate(repo_path, changed_files)
  +    print(f"[*] Pre-flight result: tier={preflight_tier} | task_type={task_type} | category_b={category_b}")
  +
  +    # FAST PATH: LOW/MEDIUM + STRUCTURE_ONLY + no Category-B.
  +    # Skips only the O(n) per-file loops (nullification, per-file AST drift).
  +    # The independent Critic subagent review still happened at planning time
  +    # (satisfying UMAGS Â§1). The verification loop itself is the Judge role.
  +    fast_path = (
  +        preflight_tier in ("LOW", "MEDIUM")
  +        and task_type == "STRUCTURE_ONLY"
  +        and not category_b
  +    )
  +    # ESCALATE: HIGH tier or Category-B â†’ external human review required.
  +    escalate = (preflight_tier == "HIGH") or category_b
  +
  +    if fast_path:
  +        print("[\u2713] PRE-FLIGHT â†’ FAST PATH: LOW/MEDIUM risk + STRUCTURE_ONLY + no Category-B.")
  +        print("    Skipping: O(n) nullification loop, per-file AST drift, cognitive LLM review.")
  +        print("    Running:  scope check + single test suite (standard Auditor/Judge steps).")
  +    else:
  +        reasons = []
  +        if preflight_tier == "HIGH":
  +            reasons.append(f"tier={preflight_tier}")
  +        if category_b:
  +            reasons.append("Category-B=true")
  +        if task_type != "STRUCTURE_ONLY":
  +            reasons.append(f"task_type={task_type}")
  +        print(f"[!] PRE-FLIGHT â†’ FULL PATH ({', '.join(reasons) if reasons else 'default'}).")
  +        if escalate:
  +            print("[!] Note: ESCALATE will be printed at completion â€” external review required.")
  +    print()
  +
       print(f"====================================================================")
       print(f"ðŸ› ï¸  BUILDER (Gemini Pro)")
       print(f"====================================================================")
  @@ -523,10 +615,15 @@ def main():
       # Programmatic Nullification Check (Anti-Test Laundering)
       # SKIPPED for STRUCTURE_ONLY tasks: content did not change, reverting a moved file
       # would only confirm that an empty/old path makes tests fail, which is trivially true.
  -    if task_type == "STRUCTURE_ONLY":
  -        print("[*] Nullification check: SKIPPED (STRUCTURE_ONLY â€” no content changed).")
  +    if task_type == "STRUCTURE_ONLY" or fast_path:
  +        print("[*] Nullification check: SKIPPED (STRUCTURE_ONLY or FAST PATH â€” no content changed / O(n) budget rule).")
  +    elif not test_failed_baseline and changed_files and preflight_tier != "HIGH":
  +        # O(n) TOKEN-BUDGET RULE: nullification scales with file count.
  +        # Only runs when pre-flight tier is HIGH; skipped at LOW/MEDIUM.
  +        print(f"[*] Nullification check: SKIPPED (pre-flight tier={preflight_tier} â€” O(n) budget rule; applies at HIGH only).")
  +        nullification_passed = True
       elif not test_failed_baseline and changed_files:
  -        print("[*] Running programmatic Nullification check...")
  +        print("[*] Running programmatic Nullification check (O(n) â€” pre-flight tier HIGH)...")
           nullification_passed = True
           
           for f in changed_files:
  @@ -574,10 +671,10 @@ def main():
                       
           if not nullification_passed:
               auditor_verified = False
      # Programmatic UMAGS AST-based Rules and Residual Risk checks
  -    # SKIPPED for STRUCTURE_ONLY: relocated-but-unmodified files produce only
  +    # SKIPPED for STRUCTURE_ONLY or FAST PATH: relocated-but-unmodified files produce only
       # false-positive drift noise (every moved function appears "new").
  -    if task_type == "STRUCTURE_ONLY":
  -        print("[*] AST compliance + Residual Risk checks: SKIPPED (STRUCTURE_ONLY â€” no content changed).")
  +    if task_type == "STRUCTURE_ONLY" or fast_path:
  +        print("[*] AST compliance + Residual Risk checks: SKIPPED (STRUCTURE_ONLY or FAST PATH â€” no content changed).")
           R = 0
       else:
           print("[*] Running UMAGS programmatic AST compliance checks...")
  @@ -639,9 +736,10 @@ def main():
   
   
       # Cognitive Auditor check (evaluates code patch and task bounds, walkthrough is strictly excluded to prevent bias)
  -    # SKIPPED for STRUCTURE_ONLY: no logic changed, semantic review of relocated code is noise.
  -    if task_type == "STRUCTURE_ONLY":
  -        print("[*] Cognitive Auditor review: SKIPPED (STRUCTURE_ONLY â€” no content changed).")
  +    # SKIPPED for STRUCTURE_ONLY or FAST PATH: no logic changed, semantic review
  +    # of relocated code or low-risk structural tasks produces only noise.
  +    if task_type == "STRUCTURE_ONLY" or fast_path:
  +        print("[*] Cognitive Auditor review: SKIPPED (STRUCTURE_ONLY or FAST PATH â€” no content changed).")
       elif os.environ.get("ANTHROPIC_API_KEY") or os.environ.get("GEMINI_API_KEY"):
           print("[*] Auditor: Querying cognitive agent for semantic review (isolated from walkthrough)...")
           system_prompt = (
  @@ -894,7 +992,7 @@ Builder Walkthrough (Intended Reality):
           "builder_status": "claimed_done",
           "auditor_verdict": auditor_verdict.lower(),
           "judge_action": "accepted" if judge_approved else "rejected",
  -        "nullification_test_run": True,
  +        "nullification_test_run": not (task_type == "STRUCTURE_ONLY" or fast_path or preflight_tier != "HIGH"),
           "discrepancies_found": len(auditor_comments),
           "longitudinal_warnings": len([h for h in history_comments if "Warning" in h or "Spot" in h or "Drift" in h]),
           "residual_risk_score": R
  @@ -906,7 +1004,23 @@ Builder Walkthrough (Intended Reality):
       except Exception as e:
           print(f"[-] Telemetry write failed: {e}")
   
  +    # ESCALATE â€” printed last for maximum visibility.
  +    # Indicates external human review is required before closing this task.
  +    if escalate:
  +        print()
  +        print("====================================================================")
  +        print("ESCALATE: requires external review")
  +        _esc_reasons = []
  +        if preflight_tier == "HIGH":
  +            _esc_reasons.append(f"pre-flight tier={preflight_tier}")
  +        if category_b:
  +            _esc_reasons.append("Category-B data claims present")
  +        print(f"  Reason:  {', '.join(_esc_reasons) if _esc_reasons else 'full-ceremony task'}")
  +        print("  Action:  Paste this output into PROJECT_LOG.md 'External verification' field.")
  +        print("====================================================================")
   
   
   if __name__ == "__main__":
       main()
  +
  +
EXPECTED_OUTCOMES: 'Integrates Ultron risk engine as a pre-flight risk gate in run_verification_loop.py to dynamically select ceremony levels, skips O(n) checks on low/medium risk structural tasks, documents the protocol, and adds category-b and escalate flow.'
TEST_COMMANDS:
- 'python ultron/tests/run_tests.py'
RAW_STDOUT: |
  [+] Running Ultron Core Tests...
  [+] Recorded rating for ultron/core/risk.py by CLI_Test.
    - Rater Judgment: HIGH
    - System Computed: HIGH (Score: 88.78)
    - Agreement: MATCH
  Usage: python checks.py <file_or_dir_path>
  Usage: python failure_space.py <repo_path> <comma_separated_changed_files>
  [*] Starting calibration over 1 transactions...
  [+] Recalibration complete.
    - New Fusion Weights: w_test=0.341, w_git=0.259, w_runtime=0.141, w_human=0.059, w_test_runtime=0.094, w_git_human=0.106
RAW_STDERR: |
  ..[-] Error processing rating: Could not evaluate risks for file
  ...127.0.0.1 - - [24/Jun/2026 10:44:08] "POST /api/design-oracle HTTP/1.1" 200 -
  127.0.0.1 - - [24/Jun/2026 10:44:08] "POST /api/design-oracle HTTP/1.1" 400 -
  C:\Users\This PC\AppData\Local\Python\pythoncore-3.14-64\Lib\tempfile.py:484: ResourceWarning: Implicitly cleaning up <HTTPError 400: 'Bad Request'>
    _warnings.warn(self.warn_message, ResourceWarning)
  127.0.0.1 - - [24/Jun/2026 10:44:08] "POST /api/design-oracle HTTP/1.1" 400 -
  127.0.0.1 - - [24/Jun/2026 10:44:08] "POST /api/design-oracle HTTP/1.1" 200 -
  127.0.0.1 - - [24/Jun/2026 10:44:08] "POST /api/design-oracle HTTP/1.1" 200 -
  127.0.0.1 - - [24/Jun/2026 10:44:08] "POST /api/design-oracle HTTP/1.1" 200 -
  127.0.0.1 - - [24/Jun/2026 10:44:08] "POST /api/design-oracle HTTP/1.1" 400 -
  127.0.0.1 - - [24/Jun/2026 10:44:08] "POST /api/design-oracle HTTP/1.1" 400 -
  ...............
  ----------------------------------------------------------------------
  Ran 20 tests in 4.075s
  
  OK
SELF_AUDIT: ''
KNOWN_LIMITATIONS: 'Markov classifier false-positive generation remains an open issue for future roadmap-level updates.'
CATEGORY_B: 'false'
